/**\n * Story Risks Slide Component\n * Renders risks/challenges list\n */\n\nimport React from 'react';\nimport { StoryRisksSlide } from '@/types/learn';\nimport styles from '../story-slides.module.css';\n\ninterface StoryRisksProps {\n  slide: StoryRisksSlide;\n}\n\nconst StoryRisks: React.FC<StoryRisksProps> = ({ slide }) => {\n  const risks = slide.risks || [];\n\n  return (\n    <div className={styles['slide']} style={{ backgroundColor: slide.bg }}>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb1,\n          top: '5%',\n          right: '5%',\n        }}\n      />\n      <div className={styles['content']}>\n        <h2 className={styles['title']}>{slide.title}</h2>\n        <div className={styles['risks-list']}>\n          {risks.map((risk, idx) => (\n            <div key={idx} className={styles['risk-item']}>\n              <span className={styles['risk-number']}>⚠️ {risk.num}</span>\n              <p className={styles['risk-text']}>{risk.text}</p>\n            </div>\n          ))}\n        </div>\n      </div>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb2,\n          bottom: '10%',\n          left: '5%',\n        }}\n      />\n    </div>\n  );\n};\n\nexport default StoryRisks;\n