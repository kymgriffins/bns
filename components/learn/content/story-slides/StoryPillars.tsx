/**\n * Story Pillars Slide Component\n * Renders 4-column pillar layout (icons + text)\n */\n\nimport React from 'react';\nimport { StoryPillarsSlide } from '@/types/learn';\nimport styles from '../story-slides.module.css';\n\ninterface StoryPillarsProps {\n  slide: StoryPillarsSlide;\n}\n\nconst StoryPillars: React.FC<StoryPillarsProps> = ({ slide }) => {\n  const pillars = slide.pillars || [];\n\n  return (\n    <div className={styles['slide']} style={{ backgroundColor: slide.bg }}>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb1,\n          top: '0%',\n          right: '0%',\n        }}\n      />\n      <div className={styles['content']}>\n        <h2 className={styles['title']}>{slide.title}</h2>\n        <div className={styles['pillars']}>\n          {pillars.map((pillar, idx) => (\n            <div key={idx} className={styles['pillar']}>\n              <div className={styles['pillar-icon']}>{pillar.icon}</div>\n              <h3>{pillar.title}</h3>\n              <p>{pillar.desc}</p>\n            </div>\n          ))}\n        </div>\n      </div>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb2,\n          bottom: '0%',\n          left: '0%',\n        }}\n      />\n    </div>\n  );\n};\n\nexport default StoryPillars;\n