/**\n * Story CTA Slide Component\n * Renders call-to-action slide with buttons\n */\n\nimport React from 'react';\nimport { StoryCTASlide } from '@/types/learn';\nimport styles from '../story-slides.module.css';\n\ninterface StoryCTAProps {\n  slide: StoryCTASlide;\n  onAction: (action: string) => void;\n}\n\nconst StoryCTA: React.FC<StoryCTAProps> = ({ slide, onAction }) => {\n  const buttons = slide.buttons || [];\n\n  return (\n    <div className={styles['slide']} style={{ backgroundColor: slide.bg }}>\n      <div className={styles['content']}>\n        <div className={styles['cta-container']}>\n          <div className={styles['cta-emoji']}>{slide.emoji}</div>\n          <h2 className={styles['cta-title']}>{slide.title}</h2>\n          <p className={styles['cta-subtitle']}>{slide.subtitle}</p>\n\n          {/* CTA Buttons */}\n          <div className={styles['cta-buttons']}>\n            {buttons.map((button, idx) => (\n              <button\n                key={idx}\n                className={`${styles['cta-btn']} ${styles[`btn-${button.type}`]}`}\n                onClick={() => onAction(button.action)}\n              >\n                <span className={styles['btn-icon']}>{button.icon}</span>\n                <div className={styles['btn-content']}>\n                  <span className={styles['btn-title']}>{button.title}</span>\n                  {button.subtitle && (\n                    <span className={styles['btn-subtitle']}>{button.subtitle}</span>\n                  )}\n                </div>\n              </button>\n            ))}\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default StoryCTA;\n