/**\n * Story Bullets Slide Component\n * Renders bullet points slide\n */\n\nimport React from 'react';\nimport { StoryBulletsSlide } from '@/types/learn';\nimport styles from '../story-slides.module.css';\n\ninterface StoryBulletsProps {\n  slide: StoryBulletsSlide;\n}\n\nconst StoryBullets: React.FC<StoryBulletsProps> = ({ slide }) => {\n  const bulletItems = slide.bullets || [];\n\n  return (\n    <div className={styles['slide']} style={{ backgroundColor: slide.bg }}>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb1,\n          top: '5%',\n          right: '5%',\n        }}\n      />\n      <div className={styles['content']}>\n        <h2 className={styles['title']}>{slide.title}</h2>\n        <ul className={styles['bullets']}>\n          {bulletItems.map((bullet, idx) => (\n            <li key={idx} className={styles['bullet-item']}>\n              {bullet}\n            </li>\n          ))}\n        </ul>\n      </div>\n      <div\n        className={styles['orb']} \n        style={{\n          background: slide.orb2,\n          bottom: '10%',\n          left: '5%',\n        }}\n      />\n    </div>\n  );\n};\n\nexport default StoryBullets;\n